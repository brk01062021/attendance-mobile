import { dashboardColors } from './colors';
import { dashboardSpacing } from './spacing';
import { dashboardTypography } from './typography';
import { dashboardShadows } from './shadows';

export const dashboardTheme = {
    colors: dashboardColors,
    spacing: dashboardSpacing,
    typography: dashboardTypography,
    shadows: dashboardShadows,
};

export default dashboardTheme;
